# testdata/

Shared fixtures for integration-level tests (e.g. sample scanner output
files used to test internal/parser and internal/report). Package-local
unit test fixtures should live in that package's own `testdata/` per Go
convention instead. Empty until the relevant milestones add parsers to
test.
