# BANNIN dashboard (web/)

Placeholder. The React + Vite + Tailwind dashboard frontend is scaffolded
here starting at Milestone 17 (Dashboard), once `internal/dashboard` and
`api/server` exist for it to talk to. Scaffolding it earlier would have
nothing real to render against.
