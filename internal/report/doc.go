// Package report renders output from the normalized finding set: the
// Report envelope, a human-readable CLI summary, and report.json. HTML
// and SARIF rendering are later milestones and will consume the same
// Report.
package report
